# Copy this file to deploy/.env before starting the reference stack.
# The real .env file is ignored by Git.

# External port exposed by the Nginx/web container.
WEB_PORT=8080

# PostgreSQL database used internally by the Compose stack.
DB_NAME=developer_analytics
DB_USERNAME=developer_analytics

# Replace this value before running the stack.
DB_PASSWORD=change-me
