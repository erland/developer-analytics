services:
  backend:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    image: developer-analytics-backend:local

  worker:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    image: developer-analytics-backend:local

  web:
    build:
      context: ..
      dockerfile: frontend/Dockerfile
    image: developer-analytics-web:local
